# Copy to .env and fill in. .env is gitignored — never commit it.

# --- Telegram ---------------------------------------------------------------
# Create at https://my.telegram.org -> "API development tools" (any app name works)
TG_API_ID=
TG_API_HASH=
# The group the sermons are forwarded into: its numeric id or exact name.
# Find it with: docker compose run --rm ingest list-chats
TG_GROUP=
TG_POLL_SECONDS=300

# --- Chunking ---------------------------------------------------------------
CHUNK_SECONDS=300

# --- Whisper ----------------------------------------------------------------
# tiny | base | small | medium | large-v3 | large-v3-turbo  (bigger = more accurate, slower)
WHISPER_MODEL=medium
WHISPER_COMPUTE_TYPE=int8
WHISPER_CPU_THREADS=8
# "en" forces English; leave empty to auto-detect per chunk
WHISPER_LANGUAGE=en
# Names and words from the sermons help Whisper spell them right, e.g.
# "Sermon by Pastor ... at ... Church. Holy Spirit, Hallelujah, Amen."
WHISPER_INITIAL_PROMPT=

# Host ports (change if they clash with something already running)
POSTGRES_PORT=54329
REDIS_PORT=63790
